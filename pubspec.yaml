name: smart_resume_parser
description: A Flutter project for parsing and analyzing resumes.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^4.2.7
  permission_handler: ^11.4.0
  sqflite: ^2.4.2
  http: ^1.3.0
  url_launcher: ^6.3.1
permission_handler: ^10.2.0
path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
